{
  "name": "empowerher-backend",
  "version": "1.0.0",
  "description": "Backend API for EmpowerHer - Gender-Based Violence Reporting Platform",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "jest",
    "lint": "eslint src/",
    "build": "echo 'No build step required'",
    "kill-port": "node -e \"const { execSync } = require('child_process'); try { const output = execSync('netstat -ano | findstr :5000', { encoding: 'utf8' }); const pid = output.split(/\\s+/)[4]; if (pid) { execSync(`taskkill /PID ${pid} /F`, { stdio: 'inherit' }); console.log('✓ Port 5000 freed'); } else { console.log('Port 5000 is already free'); } } catch(e) { console.log('Port 5000 is already free'); }\"",
    "create-admin": "node scripts/create-admin.js"
  },
  "dependencies": {
    "axios": "^1.6.7",
    "bcryptjs": "^2.4.3",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "crypto": "^1.0.1",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "express-validator": "^7.0.1",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.0.3",
    "morgan": "^1.10.0",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^6.9.7",
    "socket.io": "^4.7.4",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "jest": "^29.7.0",
    "supertest": "^6.3.3",
    "eslint": "^8.56.0"
  },
  "keywords": [
    "gender-based-violence",
    "reporting-platform",
    "social-justice",
    "api",
    "nodejs",
    "express"
  ],
  "author": "EmpowerHer Team",
  "license": "MIT"
}
